# 어딧(Eodit) 보안 및 개인정보 보호

이 저장소는 어딧의 **Android 앱과 자체 호스팅 Node.js/SQLite 서버 소스**를 공개합니다. APK나 공용 가족 서버는 제공하지 않으며 Google Play에도 아직 출시되지 않았습니다. 서버는 가족이 운영자를 신뢰하고 참여하는 소규모 환경을 전제로 합니다. 인증·가족 연결·공유 동의에 따른 접근 제한이 있지만, 서버 운영자를 배제하는 종단간 암호화 서비스나 긴급 구조 시스템은 아닙니다. 소스 공개는 외부 감사·침해 방지·무중단 운영을 보장하지 않습니다.

## 신뢰 경계

- **서버와 관리자:** SQLite에는 표시 이름, 가족 관계, 위치, 안심존, 귀가·알림 상태 등이 저장됩니다. 기존 SOS API와 첨부 저장소도 남아 있습니다. 서버 관리자, 호스트 침해자, 저장소·백업 접근자는 이 데이터를 읽거나 변경할 수 있습니다.
- **앱과 휴대전화:** 기기 세션을 가진 사람은 해당 프로필로 요청할 수 있습니다. Android 앱은 Android Keystore를 이용해 로컬 세션을 암호화하고 자동 백업을 비활성화합니다. 앱 배포자와 사용자는 서명·자격 증명 보호, 화면 잠금·OS 업데이트·기기 보안을 관리해야 합니다. 루팅된 기기, 악성 접근성 서비스, 탈취된 실행 프로세스에서 비밀을 보호한다고 가정하지 마세요. APK의 소스 대응·서명·출처를 확인하며 소스 공개 자체를 바이너리 안전성 보증으로 보지 않습니다.
- **연결된 가족과 역할:** 초대는 보호자 또는 피보호자 역할을 지정합니다. 보호자는 직접 연결된 피보호자가 동의해 공유한 최근 위치·이동 기록·위치가 포함된 안전 정보만 조회할 수 있습니다. 피보호자에게 보호자 위치를 제공하지 않으며 보호자끼리·피보호자끼리도 상대 위치를 조회할 수 없습니다. 새 보호자 연결은 피보호자의 남아 있는 공유 기록 접근에 영향을 줍니다. 수신자가 이미 본 내용·스크린샷·별도 사본은 서버가 회수할 수 없습니다. 가족 연결과 역할을 위치 공유 동의로 취급하지 마세요.
- **외부 서비스:** 선택한 HTTPS 프록시나 터널 제공자가 TLS를 종료하면 평문 HTTP 내용에 접근 가능한 신뢰 주체가 됩니다. 전송 경로와 로그 정책을 검토해야 합니다. Android 앱의 NAVER 지도 요청은 기기 IP·조회 영역을 지도 제공자에게 전달합니다. SDK·지도 콘텐츠의 데이터 처리·이용약관은 프로젝트 MIT 라이선스로 대체되지 않습니다([NOTICE](NOTICE)). 지도 로고와 법적 고지 접근을 가리지 마세요.

## 기기 인증과 초대

참여는 초대와 표시 이름으로 이루어집니다. 이메일·비밀번호, 일반 회원가입·로그인·로그아웃, 이름만으로 복구하는 기능은 없습니다. 표시 이름은 신원 확인 수단이나 비밀번호가 아닙니다.

서버는 bearer 세션 토큰의 원문 대신 **SHA-256 해시**를 보관합니다. 토큰은 기기 프로필 삭제 등으로 폐기되기 전까지 지속되므로 토큰 유출은 장기 접근 권한 유출로 취급하세요. 클라이언트는 OS의 보안 저장소 등을 사용해 토큰을 보호하고 로그·백업·외부 서비스에 노출하지 않아야 합니다. 저장 시 암호화만으로 실행 중 클라이언트나 손상된 OS에서 토큰을 완전히 숨길 수는 없습니다. 서버가 토큰 해시만 저장한다는 것도 데이터베이스의 다른 개인정보를 암호화한다는 뜻이 아닙니다.

초대 링크·QR·코드는 **역할이 지정된 비공개 참여 권한**입니다. 기본 유효기간은 10분이며 한 번 사용할 수 있습니다. 같은 발급자가 새 초대를 만들면 이전 초대는 대체됩니다. 페이지를 보는 것만으로 소비되지 않아 미리보기·브라우저 기록·프록시 접근 로그에 남을 수 있습니다. 링크를 공개 게시판, 이슈, 스크린샷, 분석 도구에 보내지 마세요. 수신자·지정 역할·서버 주소를 확인한 후 전달하세요. 기존 프로필은 현재 역할과 같은 초대만 사용할 수 있습니다.

첫 기기 설정 초대는 사용자 0명인 데이터베이스에서 로컬 운영자 CLI로만 발급하며 첫 프로필을 보호자로 지정합니다. 이미 사용자가 있는 서버의 인증 복구용 우회 수단이 아닙니다. 새 프로필 역할은 초대로 지정되고 기존 프로필 역할은 앱에서 변경할 수 없습니다. 앱 삭제나 데이터 초기화는 서버 프로필을 삭제하거나 세션을 자동 폐기하지 않습니다. 모든 기기 자격 증명을 잃은 경우 기존 프로필을 이름으로 되찾을 수 없습니다.

프로필 삭제는 현재 표시 이름을 정확히 입력하는 확인 절차와 인증된 `DELETE /api/me` 요청을 사용합니다. 성공하면 해당 프로필·연결 데이터·자격 증명을 삭제/폐기하며, 첨부 삭제의 파일 I/O 실패는 서버 정리 대기열에서 재시도합니다. 삭제 확인 이름은 별도 인증 요소가 아니라 실수 방지 절차입니다. 서버 연결 실패 시 삭제 완료로 간주하지 마세요.

## 전송·프록시·배포

- 운영 환경에서는 **유효한 HTTPS** 원점을 사용하세요. 어딧 release 앱은 HTTPS만 허용하며 debug만 개발용 HTTP 서버 주소·평문 통신을 추가로 허용합니다. 서버의 `ALLOW_INSECURE_INVITES=true`는 HTTP 초대 원점을 허용하는 별도의 개발 테스트용 설정이며 전송 암호화를 제공하지 않습니다. HTTP에 실사용 토큰·개인정보를 보내거나 인증서 검증을 해제하지 마세요.
- `PUBLIC_BASE_URL`은 운영자가 명시적으로 설정합니다. 임의 Host/전달 헤더를 공용 초대 주소의 근거로 쓰지 않습니다. 본인 도메인과 인증서·DNS·리디렉션 경로를 관리하세요.
- `TRUST_PROXY=true`에서는 `X-Forwarded-For`의 마지막 항목이 유효한 IP이면 요청 제한에 사용합니다. **신뢰하는 프록시만 백엔드에 연결하고 헤더를 올바르게 설정한다는 가정**이 필요합니다. 신뢰하지 않는 클라이언트의 직접 접근이나 임의 헤더 전달을 허용하면 이 가정이 깨집니다. 직접 Node 운영은 loopback에 바인딩하고 방화벽을 구성하세요. Compose의 8080도 호스트 loopback에만 바인딩되며 같은 호스트·컨테이너 네트워크의 접근자 역시 신뢰 경계에 포함됩니다.
- 초대와 인증 API의 요청 제한은 대규모 서비스 거부 공격 방어를 보장하지 않습니다. OS·Node·컨테이너·프록시를 갱신하고 자원·디스크·인증서 만료·이상 요청을 운영자가 감시하세요.
- 선택적 APK 다운로드 경로는 **인증이 없는 공개 배포 경로**입니다. 직접 빌드·서명한 파일을 제공한다면 지도 설정·배포 권한·출처를 확인하고 비밀·개인정보를 포함하지 마세요. 저장소에는 APK를 동봉하지 않습니다. APK 없이도 서버 시작·첫 기기 초대 발급·API 테스트가 가능하며, 파일이 없거나 경로가 설정되지 않았으면 다운로드 요청은 HTTP 503을 반환합니다.

## Android 빌드·키·배포 경계

- NAVER Maps Key ID는 APK manifest에서 추출할 수 있는 클라이언트 식별자이지 서버용 비밀이 아닙니다. 본인 계정에 Android 패키지 `org.ansim.link`와 Dynamic Map을 등록하고 허용 패키지·사용량·요금 제한을 관리하세요. 계정 비밀 키나 Maps Secret Key는 앱에 넣지 않습니다.
- 키 설정 우선순위는 `-PnaverMapKeyId` → `NAVER_MAP_KEY_ID` → 무시되는 `android/local.properties`의 `naverMapKeyId`입니다. `-P`는 셸 기록·프로세스 목록에 남을 수 있습니다. 키·개인 SDK 경로·실서버 주소를 커밋하거나 CI에서 출력하지 마세요. 키 없는 빌드는 지도 설정 안내를 표시하며 지도 기능을 검증하지 않습니다.
- CI는 wrapper JAR 무결성을 검증하고 배포 ZIP은 wrapper 설정의 SHA-256으로 검증합니다. 이는 모든 전이 의존성이나 빌드 결과의 안전성을 보증하지 않습니다. Actions 커밋·의존성·SDK 변경은 upstream 출처와 고지를 검토합니다. CI에 운영 자격 증명·서명 키를 제공하지 않으며 APK도 업로드하지 않습니다.
- 기본 release 빌드는 unsigned입니다. 배포자는 별도 키로 서명·검증하고 기존 설치의 패키지·서명·버전 호환성을 지켜야 합니다. 서명이 다른 개발 APK 설치를 위해 실사용 앱을 삭제하면 기기 자격 증명을 잃습니다. 앱 데이터 초기화를 복구 수단처럼 안내하지 마세요.
- `android/store/`의 지도 캡처·스토어 소재, `distribution/`, 빌드 출력, local.properties, keystore, DB·첨부·백업·로그는 공개 대상이 아닙니다. `.gitignore`는 이미 추적되거나 강제 추가한 파일을 보호하지 않습니다.

## 보관·삭제·백업

SQLite 위치 데이터와 첨부 파일은 **서버 애플리케이션 수준 저장 암호화가 적용되지 않습니다.** 제한된 파일 권한은 암호화를 대신하지 않습니다. 호스트 디스크·백업 암호화, 최소 권한, 접근 통제, 보존 기간 및 법적 동의·개인정보 안내는 운영자 책임입니다.

위치 기록은 7일 보관 범위로 관리됩니다. 서버가 인증된 공유 중지 요청을 받아 공유 상태를 켜짐에서 꺼짐으로 바꾸면 해당 프로필의 위치 기록을 삭제하고 진행 중인 귀가 상태를 취소합니다. 클라이언트는 새 위치 전송을 멈추고 이 요청의 성공 여부를 확인해야 합니다. 오프라인에서는 서버 반영이 지연되고, 서버 정리 작업도 서버가 실행되어야 동작합니다. 공유 중지는 프로필·가족 관계·안심존·기존 SOS 등 모든 데이터를 삭제하는 기능이 아닙니다. 기존 SOS·첨부 API도 여전히 제공되므로 클라이언트 화면에 기능이 없다는 이유로 접근·보존 위험이 없다고 가정하지 마세요. 모든 종류의 데이터가 7일에 사라지는 것은 아니며, 해결되지 않은 SOS나 진행 중인 귀가 상태는 별도 생명주기를 가집니다.

삭제는 애플리케이션 데이터 접근·정리 동작이지 저장 매체의 포렌식 완전 삭제 보장이 아닙니다. SQLite WAL, 파일시스템 스냅샷, 외부 로그, 수신자 사본, 오래된 백업에 데이터가 남을 수 있습니다. 백업에도 민감한 데이터가 있으므로 접근 권한과 만료 정책을 정하고, 삭제된 프로필·세션이 오래된 백업 복원으로 되살아나는 위험을 고려하세요.

스키마 변경 전에는 서비스를 정상 종료한 일관된 전체 저장소 백업 또는 검증된 SQLite 백업 절차를 사용하세요. 실행 중 DB 본체만 복사하면 WAL·첨부와 불일치할 수 있습니다. v1/v2→v3 이전은 기존 사용자·데이터와 유효한 세션을 유지하되 기존 프로필 역할을 미선택으로 둡니다. 역할을 선택할 때까지 다른 사람 위치는 숨겨지고 위치 전송은 거부됩니다. 보호자 선택 시 해당 프로필의 기존 위치 기록을 삭제하고 진행 중인 안심귀가를 취소합니다. 이미 만료된 토큰은 되살리지 않습니다. 다운그레이드는 보장하지 않으므로 코드 버전과 백업을 함께 기록하세요.

## 취약점 비공개 제보

1. 저장소의 **Security** 탭에 **Report a vulnerability**가 실제로 표시되어 있다면 GitHub 비공개 취약점 보고 기능을 사용하세요. 이 문서는 해당 기능이 활성화되었다고 보증하지 않습니다.
2. 기능이 없다면 저장소 소유자/유지관리자가 공개 프로필 등에 명시한 **비공개 연락 수단**을 이용하세요. 연락 경로가 전혀 없다면 공개 이슈에는 취약점 상세 없이 비공개 제보 경로 개설만 요청하세요. 이 프로젝트가 제공하지 않은 이메일 주소나 연락 채널을 추측하지 마세요.
3. 영향 버전, 공격 전제, 합성 데이터로 된 최소 재현, 예상/실제 동작을 전달하세요. 타인의 실서버·휴대전화·초대 링크를 시험하지 말고 본인이 관리하는 환경에서 재현하세요.

**공개 이슈·PR·로그에 정확한 실사용 위치, 실제 가족 이름·관계, 기기 식별자, 초대 링크·코드, bearer 토큰, DB·첨부, keystore·암호·터널 토큰을 올리지 마세요.** 노출되었다면 게시물 삭제만으로 비밀이 회수되었다고 생각하지 말고 영향받은 자격 증명과 연결, 저장소·백업 접근 범위를 운영자가 점검해야 합니다. 응답 시간이나 보안 지원 기간에 대한 별도 보장은 없습니다.
